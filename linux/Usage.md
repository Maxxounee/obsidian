
## HotKeys

```zsh
# home | ctrl + a - переход в начало строки
# end | ctrl + e - переход в конец строки
# ctrl + u - очистить ввод
# ctrl + k - очистить консоль
```

## Работа с файлами и директориями

```zsh
# Показывает текущий путь относительно домашней директории
# -l - показывает полный путь
dirs -l

# Показывает полный путь
pwd

# Вывод файлов и директорий в текущей директории
# -l - вывод служебной информации
# -a - вывод скрытых файлов и директорий
ls -la

# Создание директории
# -p - рекурсивное создание
mkdir -p /one/two/folderName

# Создание пустого файла
touch fileName.txt

# Создание/редактирование файла с помощью редактора nano
nano fileName.txt

# Вывод содержимого файла в консоль. Подходит для маленьких файлов
cat fileName.txt

# Вывод определенного текста из файла
cat fileName.txt | grep "Email" # Email: j.doe@example.com
cat requirements.txt | grep torch # torch 2.4.5

# Вывод первых n строк файла. По умолчанию выводит 10 строк
# -n – количество строк
# -f – fileWatcher. Автоматически выводит в консоль при изменении файла
head -n 15 -f test.txt

# Вывод последних n строк файла. По умолчанию выводит 10 строк
# -n – количество строк
# -f – fileWatcher. Автоматически выводит в консоль при изменении файла
tail -n 15 -f test.txt

# Копирование файла
# -r - флаг для копирования директории со всеми вложенными файлами
cp test.txt /documents/test/
cp -r . /documents/test/

# Удаление файла или директории
# -r - удаление вложенных файлов и директорий
# -f - не спрашивать подтверждение при удалении
rm -rf ./test

# Расположение файла, запускаемого командой (например, pip3)
which pip3

# Посмотреть все переменные path
echo $PATH

# Заменить символы
echo $PATH | tr ':' '\n'
```


## Другое

```zsh
# Очистить консоль
clear
```